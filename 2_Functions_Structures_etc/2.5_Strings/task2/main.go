package main

func main() {
	// text, _ := bufio.NewReader(os.Stdin).ReadString('\n')

	// text = strings.Replace(text, " ", "", -1)

	// str := []rune(text)

	// fmt.Println(text)

	// -3 because len - 1 - rune(\n); rune(\n) = 2 bite

}
